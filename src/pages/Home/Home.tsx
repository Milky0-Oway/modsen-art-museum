import { JSX } from 'react';
import Header from "../../components/Header/Header.tsx";

const Home = (): JSX.Element => {
    return (
        <div>
            <Header/>
        </div>
    );
}

export default Home;